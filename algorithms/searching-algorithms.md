### Linear Search

- Example would be a for loop.
- Re Big O Notion, as the sixe of the array grows, so does n
- O(n) - linear search

### Binary Search

- Divide and Conquer (only for a sorted list)
- Choose a pivot point or half way point and see if your search query lies left or right of the pivot point
-
